# I8 — credential hygiene. NAMES ONLY, never values. Copy to .env (gitignored)
# and fill locally, or store in the OS keychain. Never commit real secrets.

# Anthropic API key (used only after the Candidate authorizes spend — D6).
ANTHROPIC_API_KEY=
